<%= form_tag users_path do %>
  <p><%= @name %></p>
	<%= hidden_field_tag :user_group_id, :value=>@user_group_id%>
    <p>
    <%= email_field_tag :invitation %>
  <%= submit_tag 'invite user' %></p>
  
<% end %>
<%= render partial: 'invitation', collection: @invitations %> 

